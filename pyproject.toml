[project]
name = "hello-feast"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "feast>=0.42.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
